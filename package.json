{
    "name": "simple-bot",
    "version": "0.1.0",
    "private": true,
    "description": "A thin daemon that bridges Matrix (and other platforms) to a single pi instance via RPC",
    "type": "module",
    "license": "MIT",
    "scripts": {
        "build": "tsc",
        "dev": "tsx src/main.ts",
        "start": "node dist/main.js",
        "test": "vitest run",
        "test:watch": "vitest"
    },
    "dependencies": {
        "js-yaml": "^4.1.1",
        "matrix-bot-sdk": "^0.8.0"
    },
    "devDependencies": {
        "@types/js-yaml": "^4.0.9",
        "@types/node": "^25.2.3",
        "tsx": "^4.21.0",
        "typescript": "^5.9.3",
        "vitest": "^4.0.18"
    }
}
