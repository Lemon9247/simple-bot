# Discord bot token
DISCORD_TOKEN=your-discord-bot-token

# API key for the model provider pi uses
ANTHROPIC_API_KEY=your-anthropic-api-key

# Pi working directory on the host (mounted into container)
PI_CWD=/home/you/Documents/Obsidian/Claude

# Pi config directory (optional, defaults to ~/.pi)
# PI_HOME=/home/you/.pi
